include ./font.conf
include ./keymaps.conf
include ./themes/carbonfox.conf
include ./tab_bar.conf

enabled_layouts fat:bias=80, tall:bias=65 #, horizontal, vertical

# TODO: Don't enable it until you can disable it only on yazi
# cursor_trail 1
# cursor_trail --when-focus-on title:yazi 0

wheel_scroll_multiplier 1.0
wheel_scroll_min_lines 1
touch_scroll_multiplier 1.0

# ------------------------ kitty-scrollback ------------------------
allow_remote_control yes
listen_on unix:/tmp/kitty
shell_integration enabled
# kitty-scrollback.nvim Kitten alias
action_alias kitty_scrollback_nvim kitten ~/.local/share/nvim/lazy/kitty-scrollback.nvim/python/kitty_scrollback_nvim.py
# Browse scrollback buffer in nvim
map kitty_mod+s kitty_scrollback_nvim
# Browse output of the last shell command in nvim
map kitty_mod+g kitty_scrollback_nvim --config ksb_builtin_last_cmd_output
# Show clicked command output in nvim
mouse_map kitty_mod+right press ungrabbed combine : mouse_select_command_output : kitty_scrollback_nvim --config ksb_builtin_last_visited_cmd_output
