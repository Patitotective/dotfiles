export HYPRCURSOR_SIZE=30
# Set the integrated GPU as the primary renderer as this preserves battery life. Must set the dedicated GPU as secondary for external monitors to work.
export AQ_DRM_DEVICES="$HOME/.config/hypr/intel-igpu:$HOME/.config/hypr/nvidia-gpu"
