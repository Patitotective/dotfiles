plugin {
    hyprexpo {
        columns = 3
        gap_size = 0
        bg_col = rgb(ffffff)
        workspace_method = first 1 # always show the first workspace at the top left
        skip_emtpy = false

        gesture_distance = 300 # how far is the "max" for the gesture
    }
}
