# Autostart necessary processes (like notifications daemons, status bars, etc.)

# Wallpaper
exec-once = hyprpaper

# Autostart Polkit (to manage system-wide privileges)
exec-once = systemctl --user start hyprpolkitagent

# Autostart dunst (notificiation daemon, manages system notifications)
exec-once=/usr/bin/dunst

# updates the list of environment variables
exec-once=dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP

# Inhibit shutdown when pressing the power off key, instead run wlogout
exec-once = systemd-inhibit --who="Hyprland config" --why="wlogout keybind" --what=handle-power-key --mode=block sleep infinity & echo $! > /tmp/.hyprland-systemd-inhibit
exec-shutdown = kill -9 "$(cat /tmp/.hyprland-systemd-inhibit)

# Automatic mounting
exec-once = udiskie

# Disable bluetooth
exec-once = bluetoothctl power off

# Clipboard manager
exec-once = wl-paste --type text --watch cliphist store # Stores only text data
exec-once = wl-paste --type image --watch cliphist store # Stores only image data

# Idle manager
exec-once = hypridle

# Gamma/temperature filter
exec-once = wl-gammarelay-rs
exec-once = ~/scripts/hypr/restoreBrightness.sh

# Status bar
exec-once=waybar

# exec-once = hyprswitch init &
# exec-once = hyprswitch init --show-title --size-factor 4.5 --workspaces-per-row 6 &
# $key = tab
# $reverse = shift
# bind = alt, $key, exec, hyprswitch gui --mod-key alt_l --key $key --close mod-key-release --reverse-key=mod=$reverse --switch-type workspace --filter-current-monitor && hyprswitch dispatch
# bind = alt shift, $key, exec, hyprswitch gui --mod-key alt_l --key $key --close mod-key-release --reverse-key=mod=$reverse --switch-type workspace --filter-current-monitor && hyprswitch dispatch -r
