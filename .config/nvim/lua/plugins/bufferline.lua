return {
  "akinsho/bufferline.nvim",
  opts = {
    options = {
      indicator = {
        style = "none",
      },
      -- separator_style = "thin",
      -- show_close_icon = false,
      -- hover = {
      --   enabled = true,
      --   delay = 200,
      --   reveal = { "close" },
      -- },
    },
  },
}
